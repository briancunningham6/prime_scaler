<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <title>PrimeScaler · Educational Elixir Application</title>
    
    <!-- Load TailwindCSS from CDN -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@3.3.3/dist/tailwind.min.css" />
    
    <!-- Load Phoenix from CDN -->
    <script src="https://cdn.jsdelivr.net/npm/phoenix@1.7.7/priv/static/phoenix.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/phoenix_live_view@0.19.0/priv/static/phoenix_live_view.min.js"></script>
    <script>
      // Initialize Phoenix global object if loaded from CDN
      window.Phoenix = window.Phoenix || {};
      // Ensure LiveSocket is available
      if (!window.Phoenix.LiveSocket && window.phoenix_live_view) {
        window.Phoenix.LiveSocket = window.phoenix_live_view.LiveSocket;
      }
    </script>
    
    <!-- Custom styles -->
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    
    <style>
      /* Custom grid styles for process visualization */
      .prime-grid {
        display: grid;
        grid-template-columns: repeat(10, minmax(0, 1fr));
        gap: 0.5rem;
      }
      
      .prime-cell {
        aspect-ratio: 1 / 1;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.75rem;
        font-weight: 600;
        border-radius: 0.25rem;
        transition: all 0.3s ease;
      }
      
      .prime-cell.active {
        background-color: #4ade80; /* Green 400 */
        color: #064e3b; /* Green 900 */
      }
      
      .prime-cell.idle {
        background-color: #e5e7eb; /* Gray 200 */
        color: #6b7280; /* Gray 500 */
      }
      
      /* Grid container */
      .grid-container {
        overflow: auto;
        max-height: 24rem;
      }
    </style>
    
    <!-- Phoenix dependencies - using unpkg CDN which is more reliable -->
    <script src="https://unpkg.com/phoenix@1.7.7/priv/static/phoenix.min.js"></script>
    <script src="https://unpkg.com/phoenix_live_view@0.19.0/priv/static/phoenix_live_view.min.js"></script>
    
    <!-- Ensure Phoenix and LiveView are properly initialized -->
    <script>
      // Make sure Phoenix is defined
      window.Phoenix = window.Phoenix || {};
      
      // Create the LiveView namespace if it doesn't exist
      if (typeof window.Phoenix.LiveView === 'undefined') {
        window.Phoenix.LiveView = {};
      }
      
      // Assign the LiveSocket from phoenix_live_view global to Phoenix.LiveSocket
      if (typeof window.phoenix_live_view !== 'undefined' && 
          typeof window.phoenix_live_view.LiveSocket !== 'undefined') {
        window.Phoenix.LiveSocket = window.phoenix_live_view.LiveSocket;
      }
    </script>
    
    <!-- Application JS -->
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}></script>
  </head>
  <body class="h-full bg-gray-100">
    <%= @inner_content %>
  </body>
</html>
